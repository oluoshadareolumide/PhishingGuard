{
  "name": "phishguard",
  "version": "1.0.0",
  "description": "AI-powered phishing & scam detection — lightweight full-stack app",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "setup": "npm install && cd server && npm install && cd ../client && npm install && echo '\\n✅ Setup complete!'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["phishing", "cybersecurity", "scam-detection", "ai", "anthropic"],
  "license": "MIT"
}
